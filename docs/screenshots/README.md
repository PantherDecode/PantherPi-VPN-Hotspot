Drop real PNG/JPG screenshots here (e.g. `dashboard.png`, `hotspot.png`, `vpn.png`,
`captive-portal.png`) and uncomment the matching `![...]()` lines at the top of the main
[README.md](../../README.md).
